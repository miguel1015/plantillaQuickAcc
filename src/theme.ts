import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#d44134",
    },
    secondary: {
      main: "#efe2d1",
    },
    background: {
      default: "white",
    },
  },
});

export default theme;
