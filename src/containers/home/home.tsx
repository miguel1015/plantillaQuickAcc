import { Typography } from "@mui/material";

const Home = () => {
  return (
    <div>
      <Typography variant="h3" color="primary">
        Aquí va lo que sea
      </Typography>
    </div>
  );
};

export default Home;
